# create-nextdevtpl

Generate a standalone NextDevTpl application from an official preset or a
custom module and service selection.

```bash
pnpm dlx create-nextdevtpl my-app --preset saas
```

Run `create-nextdevtpl --help` for all options.
