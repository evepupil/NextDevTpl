review 进度：首次全库基线审计已完成；审查快照 commit：`be3fa84`；下次增量 review 从该 commit 之后开始。

## 迄今未解决的问题

### P1

- `P1-001` 生成器的 `custom + dashboard-only` 组合会生成缺失 auth/mail 导入的项目。
- `P1-002` Dashboard、Support、Footer 和 Server Action 的内部导航/重定向会丢失 locale。
- `P1-003` 用户 `banned` 状态没有接入会话和 Action 拒绝。
- `P1-004` 预签名上传没有可伪造 fileSize/MIME 的运行时校验和真实大小约束。
- `P1-005` 公开注册奖励 Action 可重复调用并向任意已知用户发放积分。
- `P1-006` 购买积分 Action 只检查登录，没有支付验证。
- `P1-007` 并发积分消费使用旧余额/批次快照，可能造成账实不一致。
- `P1-008` 订阅积分幂等检查存在竞态，来源引用没有数据库唯一约束。
- `P1-009` 支付 Webhook 忽略事件 ID，重放会重复副作用和运营事件。
- `P1-010` 积分发放失败被吞掉，支付 Webhook 仍返回 200。
- `P1-011` 续期事件早于订阅落库时会被确认并丢弃。
- `P1-012` 一次性支付 Checkout 没有对应的 Webhook 履约。
- `P1-013` 并发 Checkout 缺少创建锁/幂等保护，可能产生多个订阅。
- `P1-014` 英文 Support 页面硬编码中文。
- `P1-015` 遥测 Schema 不限制事件注册表和事件属性口径。
- `P1-016` 撤回分析同意后没有停用/清理 Google Analytics。
- `P1-017` `analytics:noop` 仍可能加载 Google Analytics。
- `P1-018` Cloudflare 部署预检把普通 vars 当成远端 Secret。
- `P1-019` Cloudflare Rate Limiting namespace ID 固定，多个项目共享计数器。

### P2

- `P2-001` Checkout 接受未登记的 `priceId`。
- `P2-002` 年付积分通过周期天数猜测。
- `P2-003` Storage `readObject` 把整个对象缓冲进 Worker 内存。
- `P2-004` generator catalog 没有验证 source/route/env/adapter 闭包。
- `P2-005` 中文独有博客会生成不存在的英文 alternate URL。
- `P2-006` 公开 Newsletter Action 没有应用层限流。
- `P2-007` Newsletter 退订和状态查询不要求邮箱所有权。
- `P2-008` logger/Sentry 旁路没有统一递归脱敏。
- `P2-009` 遥测适配器失败没有默认可观测出口。
- `P2-010` 认证页面列表存在实现漂移。
- `P2-011` 支付回调 URL 可由客户端任意传入。

## 已拍板但暂不修改

- 本轮是首次全库基线审计，问题以 `be3fa84` 完整代码树为准，不把工作区未提交改动纳入结论。
- 路线图中明确标为未开始的 M2-M7 能力不作为缺陷；本轮只审查已经存在的代码和对外承诺。
- Cloudflare 数据库是否必须迁移 Hyperdrive、Storage 流式读取是否已经影响现有业务路径，需要后续真实部署/压力验证；当前保留为 P2 风险，不把未证实内容升级为 P1。
- 本轮没有发现 P0。
