review 进度：基线审计结尾 commit：`be3fa84`；本轮无需业务决策的修复已完成并通过门禁，修复提交落地后下次 review 从该提交之后开始。

## 迄今未解决的问题

### P1

- `P1-013` 并发 Checkout 缺少创建锁和幂等保护。需要先确定同一用户是否只允许一个活跃订阅，以及历史订阅和重复 Checkout 的业务规则。
- `P1-015` 遥测事件需要版本化注册表和按事件约束属性。需要先确定注册表归属、兼容策略和模块 manifest 的约束边界。
- `P1-019` Cloudflare Rate Limiting namespace ID 需要项目间隔离。需要先确定由生成器自动分配账户级 namespace，还是由用户显式提供并承担资源管理。

### P2

- `P2-003` Storage `readObject` 当前返回完整 `Uint8Array`。是否改成流式契约会影响 R2、S3 适配器和调用方，需要先结合真实 Worker 部署与大对象业务路径决定。
- `P2-007` Newsletter 退订和状态查询缺少邮箱所有权证明。需要先确定一次性 token、确认邮件或登录用户授权中的产品流程。

## 已拍板但暂不修改

- 本轮是首次全库基线审计，问题以 `be3fa84` 完整代码树为准，不把当时工作区未提交改动纳入结论。
- 路线图中明确标为未开始的 M2-M7 能力不作为缺陷；本轮只审查已经存在的代码和对外承诺。
- Cloudflare 数据库是否必须迁移 Hyperdrive、Storage 流式读取是否已经影响现有业务路径，需要后续真实部署和压力验证；当前保留为 P2 风险，不把未证实内容升级为 P1。
- 本轮没有发现 P0。
