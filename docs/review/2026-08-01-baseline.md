# 首次全库基线审计

## 审查范围

- 审查类型：首次全库基线审计
- 起点 commit：`3a66cf6833740dae9319678cfd4bf427acd6e6fa`
- 终点 commit：`be3fa84476ef269bfd168e5416a769cc2e069715`
- 基线语义：审查终点 commit 的完整代码树，Git 历史用于定位问题来源；不把本次审查限制为增量 diff。
- 工作区：存在用户未提交改动。遥测、生成器、文档和测试相关改动均不纳入本次结论。
- 协作方式：六个低成本只读子 agent 按 feature base 分域审查，主会话负责证据复核、去重和定级。

## 结论

当前基线没有发现 P0。本轮已完成所有不需要业务取舍的修复，并通过当前项目门禁；剩余 5 项涉及并发产品策略、事件契约、Cloudflare 资源隔离、流式存储契约和邮箱所有权验证，需业务决策后处理。

以下问题都以 `be3fa84` 的代码为证据；状态按本轮修复结果更新。

## P1 问题

### P1-001 生成器允许生成无法通过类型检查的 dashboard-only 项目

状态：已解决，修复提交为 `3418d7f`。

- 位置：`packages/create-nextdevtpl/src/selection.ts:76-80`、`packages/create-nextdevtpl/src/generate.ts:451-456,719-742`、`src/lib/auth/index.ts:4-8`
- 触发：执行 `--preset custom --modules dashboard`。现有生成器测试也把这个组合当作合法输入。
- 影响：选择结果只有 `shared,dashboard`，生成器会删除 `auth` 和 `mail` feature，但重写后的 Dashboard layout 仍导入 `@/lib/auth/server`，而 `src/lib/auth/index.ts` 无条件导入已删除的 `@/features/mail/server`。生成项目无法完成 `typecheck`/`build`。
- 建议方向（基线时）：把认证作为 dashboard 的真实依赖并保留完整闭包，或生成无需认证的 dashboard；所有 custom 组合都必须对生成结果执行 typecheck。
- 修复结果：选择解析器现在拒绝没有 `auth` 的认证模块组合；公开 Dashboard 生成不再导入认证代码，
  同时裁剪认证路由、API、依赖和 safe-action，并为空 Schema 输出 `export {}`。`auth:none + analytics:none`
  生成项目已完成冻结安装、初始迁移生成、typecheck 和生产 build。

### P1-002 内部导航和认证重定向会丢失当前 locale

状态：已解决。

- 位置：`src/features/dashboard/components/sidebar.tsx:3-5,77-89,102-172`、`src/app/[locale]/(dashboard)/dashboard/support/page.tsx:3-4,25-26,93-121`、`src/app/[locale]/(dashboard)/dashboard/support/[id]/page.tsx:3-4,38-40,139`、`src/app/[locale]/(dashboard)/dashboard/support/new/page.tsx:3-5,78-83,168-172`、`src/features/marketing/components/footer.tsx:2,16-83`、`src/lib/safe-action.ts:1-3,78-90`
- 触发：用户在 `/zh` 页面点击 Dashboard、设置、工单、首页、法律页，或在中文页面提交 Action 时会话过期。
- 影响：这些路径使用 `next/link`、`next/navigation` 或硬编码 `redirect("/sign-in")`，生成的 URL 没有 locale 前缀。没有可用语言 Cookie 时会由中间件按默认语言处理，用户被带到英文页。
- 建议方向：统一使用 `@/i18n/routing` 的导航和 redirect，并为 Server Action 传递当前 locale/callback URL。

### P1-003 管理员封禁不会拒绝已有会话或后续请求

状态：已解决。

- 位置：`src/features/support/actions/admin-users.ts:193-201`、`src/lib/auth/index.ts:58-77`、`src/lib/safe-action.ts:78-118`
- 触发：管理员将用户的 `banned` 设置为 `true` 后，用户继续使用旧会话或重新登录。
- 影响：`banned` 只写入用户表并作为自定义字段暴露，Middleware、Better Auth 配置、`protectedAction` 和 `adminAction` 都没有检查它。被封禁用户仍可访问 Dashboard 和普通 Server Action。
- 建议方向：在认证创建/读取会话和服务端 Action 的共同边界拒绝封禁用户，并覆盖旧会话失效测试。

### P1-004 预签名上传的大小和 MIME 限制可被伪造请求绕过

状态：已解决。

- 位置：`src/app/api/upload/presigned/route.ts:36-81`、`src/adapters/storage/s3-compatible.ts:89-102`
- 触发：提交缺少 `fileSize`、负数、`NaN` 或伪造的 `fileSize`，再使用返回 URL 上传更大的对象；也可传入与扩展名不匹配的 `contentType`。
- 影响：请求体只有 TypeScript 类型断言，`fileSize` 只检查 `> MAX_FILE_SIZE`，预签名的 `PutObject` 也没有绑定 `ContentLength`。10 MB 约束和类型约束都不是服务端可验证的限制，可能导致存储费用和不受控内容进入对象存储。
- 建议方向：运行时校验请求结构、有限非负大小和允许 MIME；在适配器契约中传递并签名大小约束，或改用服务端上传/完成上传校验。

### P1-005 公开注册奖励 Action 可被重复调用并向任意用户发放积分

状态：已解决。

- 位置：`src/features/credits/actions.ts:35-80`、`src/features/credits/grant.ts:25-86`、`src/features/credits/index.ts:1-2`
- 触发：未登录调用导出的 `grantRegistrationBonus`，传入自己的或其他已知用户 ID，重复提交请求。
- 影响：该 Action 使用公开 `actionClient`，只校验 `userId` 字符串，没有登录、目标用户归属或一次性凭证检查；每次都会创建批次、交易并增加余额。
- 建议方向：删除公开入口，改为认证生命周期内部调用；使用不可重复的奖励来源引用和数据库约束。

### P1-006 购买积分 Action 只检查登录状态，未验证支付

状态：已解决。

- 位置：`src/features/credits/actions.ts:318-347`
- 触发：任意已登录用户直接调用导出的 `purchaseCredits`，自行填写 `amount`、`paymentId` 和过期时间。
- 影响：代码注释称该 Action 只能由支付 Webhook 调用，但实际使用 `protectedAction` 并从 feature index 导出，没有支付服务端验证。用户可以绕过 Checkout 直接获得任意数量积分。
- 建议方向：移除前端可调用的 Action 入口；履约函数只接受已验证 Webhook/内部调用的不可伪造凭证，并增加授权集成测试。

### P1-007 并发积分消费使用旧余额和批次快照

状态：已解决。

- 位置：`src/features/credits/consume.ts:32-49,52-89,122-130`
- 触发：同一用户同时发起两个消费请求，二者在同一个数据库事务隔离级别下读取同一余额和批次。
- 影响：两个请求都可能通过余额检查并写入消费交易；批次更新采用相同旧值，余额更新写入旧余额减去单次金额，而 `totalSpent` 却累加两次，造成积分余额、批次和交易账不一致，并可能实际超扣。
- 建议方向：按用户/批次加行锁或使用带条件的原子更新，统一在事务内校验受影响行数，并增加并发消费测试。

### P1-008 订阅积分的幂等检查存在竞态且数据库没有唯一约束

状态：已解决。

- 位置：`src/app/api/webhooks/payment/route.ts:353-375,396-417`、`src/db/schema/credits.ts:51-64`、`src/features/credits/grant.ts:75-86`
- 触发：同一期订阅 Webhook 并发重试，两个请求都先查不到相同 `sourceType + sourceRef` 的批次。
- 影响：两个请求都能插入积分批次和交易并增加余额。`sourceRef` 只是普通可空字段，没有唯一约束，支付重放可能重复发放订阅积分。
- 建议方向：用数据库唯一约束/幂等键和冲突处理保护来源引用，并把检查、发放、结果返回放进同一可重试事务。

### P1-009 支付 Webhook 忽略事件 ID，重放会重复执行业务和运营事件

状态：已解决。

- 位置：`src/core/services/payment.ts:77-83`、`src/app/api/webhooks/payment/route.ts:36-45,56-132,154-177`
- 触发：供应商重试或人工重放同一个已验签事件。
- 影响：`PaymentWebhookEvent.id` 已在契约中存在，但路由没有持久化或检查它。Checkout 完成、订阅创建、取消等日志和 telemetry 每次都会再次发送；订阅状态写入和其他副作用也会重复执行。
- 建议方向：增加 Webhook 事件接收表或等价幂等存储，先原子占用事件 ID，再执行可重试履约。

### P1-010 积分发放失败后 Webhook 仍返回 200

状态：已解决。

- 位置：`src/app/api/webhooks/payment/route.ts:44-52,396-441`
- 触发：`grantCredits` 因数据库、事务或运行时错误失败。
- 影响：`grantSubscriptionCredits` 捕获错误后只记录日志，不重新抛出；外层路由返回 `{ received: true }`。支付供应商会认为事件已处理，不再重试，用户可能永久丢失已购买的积分。
- 建议方向：区分已处理、可重试失败和不可重试失败；可重试履约失败必须返回非 2xx 或进入持久化重试队列。

### P1-011 续期事件早于订阅落库时会被成功确认并丢弃

状态：已解决。

- 位置：`src/app/api/webhooks/payment/route.ts:180-198,263-272,326-333`
- 触发：`subscription.renewed/paid` 先于 `subscription.active` 到达，且本地还没有对应订阅记录。
- 影响：更新本地订阅没有命中任何行，随后查不到用户 ID，函数直接返回；路由仍返回 200。该续期事件不会再次交付，后续如果没有重新发送激活事件，本期积分不会发放。
- 建议方向：对未知订阅返回可重试失败，或先持久化原始事件并在订阅补齐后重放履约。

### P1-012 一次性支付没有对应的 Webhook 履约

状态：已解决。

- 位置：`src/app/api/webhooks/payment/route.ts:93-132`、`src/features/credits/actions.ts:380-437`
- 触发：用户通过 `PaymentType.ONE_TIME` 或积分套餐 Checkout 完成一次性付款。
- 影响：Checkout 完成处理只更新 customer，并且只有 `data.subscription` 存在时才写订阅；积分购买 Checkout 的 metadata (`type: credit_purchase`) 没有被处理，已付款用户不会获得积分。
- 建议方向：按一次性商品建立明确的价格到履约项映射，以 Webhook 事件 ID 和支付对象 ID 做幂等发放。

### P1-013 并发 Checkout 检查无法阻止用户创建多个订阅

状态：已解决，按 2026-08-02 确认的单订阅生命周期方案实现。

- 位置：`src/features/payment/actions.ts:35-50,74-86`、`src/db/schema/subscription.ts:5-18`
- 触发：同一用户快速重复点击订阅按钮或并发提交两个 Action。
- 影响：两个请求都可能读到“没有活跃订阅”，随后各自创建外部 Checkout。订阅表没有 `userId` 唯一约束，也没有订阅创建锁，用户可能被创建多个外部订阅并产生重复扣款。
- 建议方向：用数据库状态/幂等键和短期 Checkout 锁保护创建流程，并按业务决定是否允许历史记录后新增订阅。

- 修复结果：`subscription.user_id` 现在有唯一约束，`subscription_checkout` 用用户级短期锁、请求
  ID 和 Checkout ID 保护并发创建；同一价格的有效 Checkout 会复用。计划升级调用供应商的立即
  补差价更新，降级保留当前价格和权益到下一周期，取消使用周期末模式并提供恢复入口；变化写入
  `subscription_history`，Webhook 在下一周期开始时应用待生效价格。迁移 `0007` 会先清理旧库中
  同一用户的重复当前记录，再建立唯一索引。

### P1-014 英文 locale 的 Support 页面仍整页显示中文

状态：已解决。

- 位置：`src/app/[locale]/(dashboard)/dashboard/support/page.tsx:85-135`、`src/app/[locale]/(dashboard)/dashboard/support/[id]/page.tsx:145-201`、`src/app/[locale]/(dashboard)/dashboard/support/new/page.tsx:84-177`
- 触发：英文用户访问 `/en/dashboard/support`、新建工单或查看工单详情。
- 影响：标题、按钮、空状态、错误提示、状态辅助文字和日期格式直接写死中文，Support manifest 的翻译声明没有覆盖这些页面。
- 建议方向：把页面文案和日期格式迁移到 locale message，补充 en/zh 页面级集成测试。

### P1-015 遥测事件契约没有真正限制事件字典和属性口径

状态：待业务决策。

- 位置：`src/core/services/telemetry.ts:34-40,121-143`
- 触发：调用方传入格式合法但未登记的事件名或任意业务属性。
- 影响：Schema 只限制命名格式、版本、通用 JSON 和属性键长度，没有事件注册表或按事件区分的属性 Schema。标准事件文档宣称 feature manifest 登记事件，代码无法阻止拼写错误和越权属性进入指标系统。
- 建议方向：建立版本化事件注册表，按事件验证属性，并让模块 manifest 与实现共享可测试的契约。

### P1-016 撤回分析同意后没有停用或清理 Google Analytics

状态：已解决。

- 位置：`src/features/analytics/components/analytics.tsx:19-61`、`src/features/marketing/components/cookie-consent.tsx:73-81`
- 触发：用户先接受全部 Cookie，GA 加载后再改为拒绝分析。
- 影响：同意变化只改变组件渲染状态，没有禁用 `gtag`、清除分析 Cookie、移除脚本或清空发送队列。已加载的 GA runtime 可能继续保留或发送数据。
- 建议方向：采用 GA consent mode 的默认拒绝/更新机制，并验证撤回后的网络请求、Cookie 和跨标签页状态。

### P1-017 `analytics:noop` 不能阻止现有 Google Analytics 数据流

状态：已解决。

- 位置：`packages/create-nextdevtpl/src/generate.ts:675-711`、`src/adapters/registry.ts:123-131`、`src/features/analytics/components/analytics.tsx:19-61`
- 触发：生成项目同时选择 `analytics` 模块、`analytics:noop`，并设置 `NEXT_PUBLIC_GA_ID`、接受分析 Cookie。
- 影响：noop 只替换服务端 telemetry adapter；生成器仍按模块是否选中来渲染 `<Analytics />`，组件直接加载 Google Analytics。适配器选择与实际外部数据流不一致。
- 建议方向：把浏览器分析也纳入 adapter 选择，或让 noop 明确禁用 GA 组件和相关配置。

### P1-018 Cloudflare 部署预检把普通变量当成远端 Secret

状态：已解决。

- 位置：`cloudflare/preflight.mjs:135-177`、`packages/create-nextdevtpl/src/generate.ts:288-370`
- 触发：使用 Wrangler `vars` 或 Dashboard 普通环境变量配置 `DATABASE_URL`、`BETTER_AUTH_URL`、`NEXT_PUBLIC_APP_URL` 等值后执行 `pnpm cf:deploy`。
- 影响：部署预检只读取 `wrangler secret list`，把 `preflight-config.json` 中所有 `required` 名称都与 Secret 名称比较，合法的普通变量配置会被报告为缺失并阻止部署。
- 建议方向：在生成配置中区分 Secret、vars 和公开配置；远端检查分别读取对应来源，避免把公开 URL 当 Secret。

### P1-019 Cloudflare Rate Limiting namespace ID 在多个项目间固定共享

状态：待业务决策。

- 位置：`packages/create-nextdevtpl/src/generate.ts:53-60,276-282`
- 触发：同一 Cloudflare 账户部署两个或更多启用 `rate-limit:cloudflare-rate-limit` 的生成项目。
- 影响：所有项目都使用 `1001` 到 `1006`。相同 namespace 会共享计数器，一个项目的登录、上传或支付请求会消耗另一个项目的限额，破坏项目间隔离。
- 建议方向：为每个生成项目分配账户内唯一 namespace，或要求用户显式提供 namespace 并在预检时验证隔离策略。

## P2 问题

### P2-001 Checkout 接受未登记的 priceId

状态：已解决。

- 位置：`src/features/payment/actions.ts:23-29,49-50,74-85`、`src/config/payment.ts:242-258`
- `findPlanByPriceId` 返回空计划时，Action 仍把客户端提供的 `priceId` 交给支付供应商，只把 metadata 的计划写成 `unknown`。可能创建无法履约的外部商品 Checkout，或让用户购买不受模板配置管理的商品。

### P2-002 年付积分通过周期天数猜测，未校验明确价格周期

状态：已解决。

- 位置：`src/app/api/webhooks/payment/route.ts:347-391`、`src/config/payment.ts:23-30,72-124`
- 代码用 `periodDays > 60` 推断年付并发放 `monthlyCredits * 12`。应根据已登记的 price interval 或供应商商品周期判断，避免自定义周期、试用或数据异常触发错误配额。

### P2-003 Storage readObject 会把整个对象缓冲进 Worker 内存

状态：待业务决策。

- 位置：`src/core/services/storage.ts:12-16,35-41`、`src/adapters/storage/r2-binding.ts:74-89`、`src/adapters/storage/s3-compatible.ts:117-133`
- R2 使用 `arrayBuffer()`、S3 使用 `transformToByteArray()`，契约最终只能返回完整 `Uint8Array`。当前业务主要走签名 URL，尚未确认已影响线上路径；这是大对象调用时触及 Worker 内存上限的适配器风险。

### P2-004 生成器 catalog 校验不足，错误 source/route/env 可以通过加载

状态：已解决。

- 位置：`packages/create-nextdevtpl/src/catalog.ts:23-81`、`src/test/architecture/generator-catalog.test.ts:9-27`
- catalog parser 只检查字段形状，不检查 module/adapter 引用、source 文件、routes、schema、env、packages、bindings 和 runtime 的闭包；现有架构测试也没有覆盖这些关系。错误会延迟到生成项目构建时才发现。

### P2-005 中文独有博客文章会生成不存在的英文 alternate URL

状态：已解决。

- 位置：`src/app/[locale]/(marketing)/blog/[slug]/page.tsx:64-69`、`src/lib/source.ts:58-79`、`src/content/blog/zh/nextdevtpl-linuxdo-intro.mdx`
- 详情页无条件输出同一 slug 的 `/en` 和 `/zh` alternate，但该 slug 只有中文文件。访问英文 alternate 会 404，也会向搜索引擎发布错误的语言替代关系。

### P2-006 公开 Newsletter Action 没有应用层限流

状态：已解决。

- 位置：`src/features/mail/actions.ts:16-85`、`src/middleware.ts:19-75`
- `subscribeNewsletter` 使用公开 Action，Middleware 的限流规则只覆盖 API 路径。自动化请求可以持续写入和重新激活订阅，造成垃圾名单和数据库膨胀。

### P2-007 Newsletter 退订和状态查询不要求邮箱所有权

状态：待业务决策。

- 位置：`src/features/mail/actions.ts:93-174`
- 任何调用方只要知道一个邮箱即可把它退订，或查询该邮箱是否在订阅名单中。退订链接应使用一次性 token/确认邮件，状态查询也应避免返回可用于枚举的 `found` 信息。

### P2-008 日志和 Sentry 旁路入口没有使用统一递归脱敏

状态：已解决。

- 位置：`src/core/services/telemetry.ts:159-204`、`src/lib/logger/index.ts:170-206`、`src/lib/monitoring/index.ts:129-143,324-332`
- telemetry 只清洗事件 `attributes`；logger 的 context、Error message、Sentry extra 以及 `withSentryAction` 的完整 input 都可直接写入输出，存在密码、Token、邮箱和原始输入泄露风险。

### P2-009 遥测适配器失败没有默认可观测出口

状态：已解决。

- 位置：`src/core/services/telemetry.ts:231-247`、`src/services/telemetry.ts:24-35`
- 当前 service 没有配置 `onAdapterError`，`trackServerEvent` 又吞掉 Promise 错误。事件丢失时既不影响业务请求，也不会留下失败计数、原因或告警信号。

### P2-010 认证页面列表存在实现漂移

状态：已解决。

- 位置：`src/middleware.ts:108-121`、`src/lib/auth/edge.ts:65-70`
- Middleware 只把 `/sign-in`、`/sign-up` 视为已登录后应跳转的页面，而另一份认证路由配置还包含 `/forgot-password`、`/reset-password`。已登录用户访问忘记密码页的行为取决于实际使用哪份配置，策略不一致。

### P2-011 支付成功/取消回调 URL 可由客户端任意传入

状态：已解决。

- 位置：`src/features/payment/actions.ts:23-29,74-80`
- Action 只把 URL 声明为普通字符串，并将其传给支付供应商。若供应商允许该 URL，攻击者可制造完成后跳转到外部站点的 Checkout；应限制为本站路径或允许列表。

## 验证状态和测试缺口

- 未发现 P0。
- 当前工作区已按 4 个串行批次运行全部 36 个测试文件，共 367 个用例通过；串行执行是为了避免远端测试数据库连接在长时间运行后断开。
- `pnpm lint`、`pnpm typecheck`、`pnpm exec biome format .` 和 `pnpm db:generate:checked` 均通过；数据库测试仍提示 `.env.test` 的 `DATABASE_URL` 不像测试库连接。
- `pnpm --filter create-nextdevtpl run prepack` 和 `pnpm build` 均通过。生产构建仍有 Next.js middleware 弃用及 Sentry/OpenTelemetry 动态依赖警告，不影响退出码。
- `pnpm exec biome check .` 仍报告 26 个既有 import 排序问题；它们不在本轮修复范围内，且 `pnpm lint` 与只读格式检查均通过。
- 尚未运行四部署目标构建或真实 Cloudflare `wrangler deploy --dry-run`。
- 仍缺少真实路由测试：Webhook 重放/乱序/履约失败状态、预签名上传边界、封禁后旧会话、locale 跳转、英文 Support、Newsletter 滥用和支付回调 URL。
- 积分并发消费已有覆盖；仍缺少 Checkout 创建、积分发放、注册奖励和订阅 upsert 的并发测试。
- 仍缺少所有主要 preset 与 adapter/target 组合的生成项目 `typecheck`/`build` 矩阵，以及 Cloudflare vars/Secret 混合配置、Rate Limiting namespace 隔离、binding 类型和 Worker 事务生命周期验证。
- 未启动 dev server，遵循仓库 AGENTS.md 的明确要求。

## 交接

本记录已同步本轮业务修复结果；待决策项保留在 `docs/review/_剩余问题.md`，后续开发会话应先确认这些项目的业务规则，再分别处理并同步两处记录。
